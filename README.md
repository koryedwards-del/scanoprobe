# Burn & Build LBA

1. Unzip on your Mac
2. Double-click **Start Burn & Build LBA.command**
3. Use Chrome when it opens. Plug in probe → Connect probe → From probe

Your formula: edit `config.json` (`mm1` = thigh, `mm2` = waist)
